{
  "name": "mess",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "dev": "tsc && live-server frontend --port=3000",
    "serve": "live-server frontend --port=3000",
    "clean": "rm -rf frontend/dist/*"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "live-server": "^1.2.2"
  }
}